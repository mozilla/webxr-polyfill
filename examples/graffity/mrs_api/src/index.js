import API from './api/index';

const api = new API();

window.onload = function () {
  const btn = document.getElementById('btn');
  btn.addEventListener('click', () => {
    //some code to test api call
  });
};
